# 文档修订汇总（review-fixes-summary）

## 1. 本次问题修复 Checklist（逐条过一遍）
- [x] 一、订单与商品闭环不完整  
  已补充 `LOCKED` 状态、单商品单活动订单约束、订单关闭后商品统一回退 `OFF_SHELF`、事务一致性规则、联动表。
- [x] 二、商品状态下可编辑规则不明确  
  已补充字段级编辑矩阵、状态下可执行操作矩阵，并明确“前端禁用 + 后端二次校验”。
- [x] 三、分类/字典体系缺失  
  已补充两级分类数据模型、分类查询接口、页面级联加载规则、初始化脚本维护策略。
- [x] 四、管理员账号来源与初始化策略缺失  
  已补充“初始化脚本预置管理员”、`SUPER_ADMIN/ADMIN` 区分、本期不支持后台创建管理员。
- [x] 五、接口 checklist 粒度不足  
  已补充必填/选填、响应字段展开、关键失败场景、并发与幂等策略、关键接口 JSON 示例。
- [x] 六、前端页面和接口映射不够清晰  
  已补充审核页/商品页/订单页动作到接口映射、状态展示规则、PC/移动差异。
- [x] 七、仓库结构定位不一致  
  已补充仓库定位说明（单仓一体，命名历史说明，未来可拆仓）。
- [x] 八、新增汇总文件要求  
  本文件已创建并记录本轮修订结果。
- [x] 九、最终交付说明要求  
  本文件包含“改了什么、改了哪些文档、是否可开工、残余风险”。

## 2. 修改过的文档
1. `docs/project-overview.md`
2. `docs/specs.md`
3. `docs/frontend-pages.md`
4. `docs/backend-api-checklist.md`
5. `docs/dir-structure.md`
6. `docs/data-model.md`
7. `docs/review-fixes-summary.md`（新增）

## 3. 每个文件改了什么
1. `project-overview.md`
   - 新增仓库定位说明。
   - 明确管理员初始化策略与 `SUPER_ADMIN/ADMIN`。
   - 补齐商品 `LOCKED` 与订单联动流程。
2. `specs.md`
   - 补齐商品-订单状态机闭环与联动表。
   - 新增商品字段级编辑矩阵。
   - 新增分类字典规格与管理员账号策略。
3. `frontend-pages.md`
   - 强化关键页面“动作 -> 接口”映射。
   - 新增审核/商品/订单状态展示规则。
   - 新增分类级联加载、按钮禁用与 PC/移动差异细则。
4. `backend-api-checklist.md`
   - 补齐必填/选填与响应字段细化。
   - 新增分类查询接口。
   - 补齐失败场景、幂等策略、并发冲突规则。
   - 增加注册、登录、审核通过/驳回、商品创建、商品上架、创建订单、完成订单示例。
5. `dir-structure.md`
   - 增加仓库定位说明。
   - 增加管理员初始化与分类初始化脚本目录约束。
6. `data-model.md`
   - 增加 `categories` 表与两级分类约束。
   - 商品状态新增 `LOCKED`，商品表新增 `active_order_id/locked_at`。
   - 订单表新增 `is_active` 与唯一约束，支撑“单商品单活动订单”。
   - 管理员表新增角色字段 `SUPER_ADMIN/ADMIN`。

## 4. 仍保留到后续版本处理
1. 管理员管理管理员页面与接口。
2. 分类管理后台（增删改）页面与接口。
3. 子账号 RBAC 可用功能。
4. 买家端、支付退款、售后等跨域能力。

## 5. 当前文档可开工评估
结论：当前文档已达到“可直接开工”标准。  
原因：角色权限、状态流转、接口字段、并发幂等、页面接口映射、数据模型已闭环且互相对齐。

## 6. 当前残余风险
1. 订单并发强依赖数据库事务与唯一约束，需在实现阶段重点压测。
2. 分类字典由脚本维护，需明确发布流程避免线上配置漂移。
3. 幂等键存储策略（TTL 与清理）需在实现时固化到工程规范。

不要只做文字润色，重点补足业务闭环、状态流转、接口粒度和开发可执行性。

## 7. 小修订补丁（本轮追加）

### 7.1 Checklist
- [x] 补齐 `merchant/account` 与 `merchant/account/password` 接口到后端 API 清单
- [x] 明确 `merchant/profile` 与 `merchant/account` 职责边界
- [x] 明确 `stock` 在本期二手单件场景的定义（固定为 1，保留字段）

### 7.2 本轮修改文件
1. `docs/backend-api-checklist.md`
2. `docs/frontend-pages.md`
3. `docs/specs.md`
4. `docs/data-model.md`
5. `docs/review-fixes-summary.md`

### 7.3 本轮变更说明
1. 在 API 文档新增账号设置接口：
   - `GET /merchant/account`
   - `PUT /merchant/account/password`
   并补齐用途、参数、响应字段、权限、失败场景。
2. 在产品与页面文档明确接口职责边界：
   - `merchant/profile`：商家主体资料 + 审核状态
   - `merchant/account`：当前登录账号资料 + 安全设置
3. 在 specs、前端页面、数据模型、API 文档统一 `stock` 约束：
   - 本期不允许 `stock > 1`
   - `stock` 固定为 `1`
   - 作为后续扩展保留字段

### 7.4 当前可开工评估
结论：经过本轮补丁，文档仍然满足并强化了“可直接进入开发实现”的标准。  
本轮补丁主要消除了接口对齐缺口与职责歧义，降低联调误差。
